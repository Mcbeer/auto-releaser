# Changelog

## 1.1.0

### @lego/dep-aware-release

- root project '.' must yield clean tree paths (package.json not ./package.json)
- extraFiles glob must include only files, not directories (EISDIR)
- extraFiles support for built artifacts in release PRs

